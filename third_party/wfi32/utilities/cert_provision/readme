./cert_create_and_prov_for_test.sh Matter-Development-PAI-noPID-Cert.pem Matter-Development-PAI-noPID-Key.pem


Environment
===================================
sudo apt-get install cmake
pip install -r requirements.txt

pip3 uninstall cryptoauthlib
pip3 install --no-cache cryptoauthlib

sudo apt-get install libudev-dev
cd /etc/udev/rules.d
sudo touch mchp-cryptoauth.rules
sudo vi mchp-cryptoauth.rules
SUBSYSTEM=="hidraw", ATTRS{idVendor}=="03eb", ATTRS{idProduct}=="2312", MODE="0666"


Procedure
=======================
cp chip-cert 
cp PAI cert and key